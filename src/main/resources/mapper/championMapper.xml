<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.project.finalProject.dao.ChampionDao">
	
	<select id="champList">
		select * from champName;
	</select>

	<select id="search">
		select * from champName where champion_name_kr like '%#{searchChamp}%' 
	</select>














</mapper>